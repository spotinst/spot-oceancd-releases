apiVersion: v1
kind: Service
metadata:
  name: stable-service-headers
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: rollout-headers-test
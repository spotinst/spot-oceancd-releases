apiVersion: apps/v1
kind: Deployment
metadata:
  {{- with .Values.operatorManager.deploymentAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  name: {{ include "oceancd-operator-manager.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: oceancd-operator-manager
    {{- include "oceancd-operator-manager.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: oceancd-operator-manager
      {{- include "oceancd-operator-manager.selectorLabels" . | nindent 6 }}
  strategy:
    type: RollingUpdate
  replicas: {{ .Values.operatorManager.replicas }}
  template:
    metadata:
      {{- with .Values.operatorManager.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "oceancd-operator-manager.selectorLabels" . | nindent 8 }}
        app: oceancd-operator-manager
        {{- range $key, $value := .Values.operatorManager.podLabels }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
    spec:
      {{- with .Values.operatorManager.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "oceancd-operator-manager.serviceAccountName" . }}
      initContainers:
      - name: regestration
        args: 
        - --registrar
        command:
        - /manager
        image: "{{ .Values.operatorManager.image.registry }}/{{ .Values.operatorManager.image.repository }}:{{ default .Chart.AppVersion .Values.operatorManager.image.tag }}"
        imagePullPolicy: {{ .Values.operatorManager.image.pullPolicy }}
        {{- if .Values.operatorManager.regestration.resources }}
        resources:
          {{- toYaml .Values.operatorManager.regestration.resources | nindent 10 }}
        {{- end }}
        env:  
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: spec.nodeName
        - name: POD_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.name
        - name: TOKEN
          valueFrom:
            secretKeyRef:
              key: token
              name: spot-oceancd-controller-token
        - name: SAAS_URL
          valueFrom:
            secretKeyRef:
              key: saasUrl
              name: spot-oceancd-controller-token
      - name: installation
        args: 
        - --installer
        command:
        - /manager
        image: "{{ .Values.operatorManager.image.registry }}/{{ .Values.operatorManager.image.repository }}:{{ default .Chart.AppVersion .Values.operatorManager.image.tag }}"
        imagePullPolicy: {{ .Values.operatorManager.image.pullPolicy }}
        {{- if .Values.operatorManager.installation.resources }}
        resources:
          {{- toYaml .Values.operatorManager.installation.resources | nindent 10 }}
        {{- end }}
        env:
        - name: TOKEN
          valueFrom:
            secretKeyRef:
              key: token
              name: spot-oceancd-controller-token
        - name: SAAS_URL
          valueFrom:
            secretKeyRef:
              key: saasUrl
              name: spot-oceancd-controller-token
        volumeMounts:
          - name: oceancd-operator-manager-config
            mountPath: /config
            readOnly: true
          {{- with .Values.operatorManager.installation.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
      containers:
      - name: manager
        args:     
        - --operations
        command: 
        - /manager
        image: "{{ .Values.operatorManager.image.registry }}/{{ .Values.operatorManager.image.repository }}:{{ default .Chart.AppVersion .Values.operatorManager.image.tag }}"
        imagePullPolicy: {{ .Values.operatorManager.image.pullPolicy }}
        livenessProbe:
          {{- toYaml .Values.operatorManager.manager.livenessProbe | nindent 10 }}
        readinessProbe:
          {{- toYaml .Values.operatorManager.manager.readinessProbe | nindent 10 }}
        securityContext:
          {{- toYaml .Values.operatorManager.manager.containerSecurityContext | nindent 10 }}
        {{- if .Values.operatorManager.manager.resources }}
        resources:
          {{- toYaml .Values.operatorManager.manager.resources | nindent 10 }}
        {{- end }}
        volumeMounts:
          - name: oceancd-operator-manager-config
            mountPath: /config
            readOnly: true
          {{- with .Values.operatorManager.manager.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- if .Values.operatorManager.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.operatorManager.nodeSelector | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.operatorManager.podSecurityContext | nindent 8 }}
      {{- if .Values.operatorManager.tolerations }}
      tolerations:
        {{- toYaml .Values.operatorManager.tolerations | nindent 8 }}
      {{- end }}
      {{- if .Values.operatorManager.affinity }}
      affinity:
        {{- toYaml .Values.operatorManager.affinity | nindent 8 }}
      {{- end }}
      {{- with .Values.operatorManager.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- range $constraint := . }}
      - {{ toYaml $constraint | nindent 8 | trim }}
        {{- if not $constraint.labelSelector }}
        labelSelector:
          matchLabels:
            app: oceancd-operator-manager
            {{- include "oceancd-operator-manager.selectorLabels" $ | nindent 12 }}
        {{- end }}
        {{- end }}
      {{- end }}
      {{- with .Values.operatorManager.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      volumes:
        - name: oceancd-operator-manager-config
          configMap:
            name: oceancd-operator-manager-config
        {{- with .Values.operatorManager.extraVolumes }}
          {{- toYaml . | nindent 8 }}
        {{- end }}